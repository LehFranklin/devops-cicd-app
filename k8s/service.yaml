apiVersion: v1
kind: Service
metadata:
  name: devops-cicd-app
  labels:
    app: devops-cicd-app
spec:
  selector:
    app: devops-cicd-app
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
